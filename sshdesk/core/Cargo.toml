[package]
name = "sshdesk-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[lib]
name = "sshdesk_core"
path = "src/lib.rs"

[[bin]]
name = "sshdesk-probe"
path = "src/bin/probe.rs"
